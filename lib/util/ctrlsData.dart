class ControlData {
  String servo1;
  String servo2;
  String motor1;
  String motor2;

  //Convert the data received from the controller to data for the ESP32

  //Polar to Cartesian
  //x = cos(theta), y = sin(theta)
  //Transmit the received data to the ESP through BLE
}
